<Page x:Class="BankManage.employee.EmployeeBase"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
	Title="EmployeeBase">
    <StackPanel>
        <DataGrid Name="dataGrid1" AutoGenerateColumns="False"
                Background="#FFDDFBF9">
            <DataGrid.Columns>
                <DataGridTextColumn Header="员工号" Binding="{Binding EmployeeNo}" Width="80" />
                <DataGridTextColumn Header="姓名" Binding="{Binding EmployeeName}" Width="80" />
                <DataGridTextColumn Header="性别" Binding="{Binding sex}" Width="80" />
                <DataGridTextColumn Header="员工类型" Binding="{Binding classid}" Width="110" />
                <DataGridTextColumn Header="电话" Binding="{Binding telphone}" Width="110" />
            </DataGrid.Columns>
        </DataGrid>
        <Button Content="保存" Width="70" Margin="0 10 0 0" Click="Button_Click_1" />
    </StackPanel>
</Page>
