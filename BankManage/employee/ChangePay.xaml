<Page x:Class="BankManage.employee.ChangePay"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
	Title="ChangePay">
    <StackPanel>
        <DataGrid Name="dataGrid1" AutoGenerateColumns="False"
                Background="#FFDDFBF9">
            <DataGrid.Columns>
                <DataGridTextColumn Header="员工号" Binding="{Binding EmployeeNo}" Width="100" />
                <DataGridTextColumn Header="姓名" Binding="{Binding EmployeeName}" Width="100" />
                <DataGridTextColumn Header="员工类别" Binding="{Binding classid}" Width="120" />
                <DataGridTextColumn Header="工资" Binding="{Binding gongzi}" Width="120" />
            </DataGrid.Columns>
        </DataGrid>
        <Button Content="保存" Width="70" Margin="0 10 0 0" Click="Button_Click_1" />
    </StackPanel>
</Page>
